# EDS 222 Post Notes

This post keeps one helper script alongside the article:

- `setup.R`: one-time setup script for preparing the Danish roadkill inputs referenced by `config.yml`.

The post itself lives in `index.qmd`. Large raw and cached data stay under `data/`.
