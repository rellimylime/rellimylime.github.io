# Hyperspectral Imaging Post Notes

This post is organized around:

- `index.qmd`: the article
- `figures/`: rendered output figures used in the post
- `data/`: source and derived hyperspectral data
- `test_render.py`: lightweight smoke test for generating the core RGB figure outside Quarto

Generated figures in `figures/` are treated as post outputs and stay with the post.
